# Node / Playwright test runner
node_modules/
test-cases/_runner/node_modules/
test-cases/_runner/artifacts/
test-cases/_runner/package-lock.json
skeleton/node_modules/
skeleton/package-lock.json
skeleton/examples/artifacts/
skeleton/.packtest/
skeleton/*.tgz

# Temp clones (study only, never committed)
_tmp_skeleton/
_tmp_huashu/
_tmp_*/
_tmp_*

# Source material (read for research, not part of the skill)
*.pdf

# Eval harness — raw case material stays local (throwaway code + screenshots, possibly private).
# Lightweight evidence (meta.json, case.md, metrics.json, SUMMARY.md) stays tracked.
test-cases/_eval/cases/*/before/
test-cases/_eval/cases/*/after/
test-cases/_eval/cases/*/control/
test-cases/_eval/cases/*/screenshots/
test-cases/_eval/cases/*/diff.patch
test-cases/_eval/node_modules/

# Editor / OS
.DS_Store
Thumbs.db

# Browser-tool review artifacts (Playwright MCP)
.playwright-mcp/
/liquid-*.png
/glass-*.png
/webgl-*.png
